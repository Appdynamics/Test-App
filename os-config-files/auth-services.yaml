apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: auth-services
spec:
  replicas: 1
  template:
    metadata:
      labels:
        name: auth-services
    spec:
       containers:
        - name: auth-services
          image: java-services
          imagePullPolicy: Never
          ports:
          - containerPort: 8080
      restartPolicy: Always

---
apiVersion: v1
kind: Service
metadata:
  name: auth-services
spec:
  selector:
    name: auth-services
  ports:
  - name: "8080"
    port: 8080
    targetPort: 8080
status:
  loadBalancer: {}
