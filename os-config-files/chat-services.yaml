apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: chat-services
spec:
  replicas: 1
  template:
    metadata:
      labels:
        name: chat-services
    spec:
      containers:
        - name: chat-services
          image: java-services
          imagePullPolicy: Never
          ports:
          - containerPort: 8080
      restartPolicy: Always

---
apiVersion: v1
kind: Service
metadata:
  name: chat-services
spec:
  selector:
    name: chat-services
  ports:
  - name: "8080"
    port: 8080
    targetPort: 8080
status:
  loadBalancer: {}
